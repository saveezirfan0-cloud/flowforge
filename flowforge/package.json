{
  "name": "flowforge-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FlowForge — multi-flow visual workflow builder, deployed on Vercel with KV storage, cron scheduling, and webhook triggers.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "@vercel/functions": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
